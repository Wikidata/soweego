#!/usr/bin/python

# file locations

assets_directory = 'assets'
temporary_directory = '{0}/temp'.format(assets_directory)
log_file = '{0}/errors.log'.format(assets_directory)
dump_states = '{0}/dump_states.json'.format(assets_directory)
db_credentials = '{0}/db_credentials.json'.format(assets_directory)

# keys

last_modified_key = 'last-modified'

# DB

db_name = 'soweego'